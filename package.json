{
  "name": "krkn-website",
  "version": "1.0.0",
  "description": "Krkn chaos engineering documentation website",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "express-rate-limit": "^7.1.5",
    "dotenv": "^16.3.1",
    "axios": "^1.6.2",
    "marked": "^10.0.0",
    "cheerio": "^1.0.0-rc.12",
    "natural": "^6.10.0",
    "fuse.js": "^7.0.0",
    "express-validator": "^7.0.1",
    "winston": "^3.11.0"
  },
  "scripts": {
    "build": "hugo --gc --minify",
    "build:preview": "hugo --gc --minify --buildDrafts --buildFuture",
    "build:production": "hugo --gc --minify",
    "dev": "hugo server --buildDrafts --buildFuture"
  }
}